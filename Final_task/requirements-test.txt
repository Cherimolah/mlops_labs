# Зависимости только для тестов и CI (не нужны в рантайме приложения).
# Версии совместимы с FastAPI 0.115 / pandas 2.2 / scikit-learn 1.8 из requirements.txt.
pytest>=8.0
httpx>=0.27          # требуется fastapi.testclient (Starlette TestClient)
